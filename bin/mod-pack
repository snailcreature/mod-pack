#!/usr/bin/env node

const yargs = require('yargs');
const { hideBin } = require('yargs/helpers');

const argv = yargs(hideBin(process.argv));

argv
.command('init', 'Create the required files', (yargs) => {return yargs}, () => {
  require('../scripts/init.js');
})
.parse();